请以JSON格式返回结果，严格按照以下数据结构：

模型名称: {{ model_name }}
{% if description %}
描述: {{ description }}
{% endif %}

JSON Schema:
{{ schema_json }}

字段说明:
{% for field_name, field_info in fields.items() %}
- {{ field_name }} ({{ field_info.type }}) {% if field_info.required %}(必填){% else %}(可选){% endif %}: {{ field_info.description }}
{% endfor %}

请确保返回的JSON格式完全符合上述schema，并且可以被 {{ model_name }}.model_validate() 成功解析。
只返回JSON内容，不要添加任何其他文字说明或markdown格式。
