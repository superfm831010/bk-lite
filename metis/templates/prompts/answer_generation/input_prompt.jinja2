以下是用于生成答案的上下文：
----------------
{{context}}
----------------
请根据上述上下文，为以下文本生成一个 **答案字典**，使用指定的 JSON 格式：
----------------
{{text}}
----------------

要求：
1. 答案必须基于 **{{context}}** 提供的内容，确保准确并尊重事实。
2. **不能使用指代词（如“它”“这个”“该问题”），必须明确写出对象或场景。**
   - ✅ 正例：Kubernetes 节点在 CPU 利用率达到 90% 时无法触发自动扩容，是因为 HPA 策略阈值设置为 95%。
   - ❌ 反例：这是因为 **它** 的阈值设置过高。
3. 答案应当清晰、具体，可独立存在，避免含糊或重复。
4. 输出为 JSON 对象
{% if extra_prompt %}
附加说明：
{{extra_prompt}}
{% endif %}