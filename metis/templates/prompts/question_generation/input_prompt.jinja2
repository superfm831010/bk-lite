请根据以下文本生成问题，使用指定的 JSON 格式：
----------------
{{text}}
----------------
生成 {{size}} 个准确，尊重事实问题

要求：
1. **不能使用指代词（如“它”“这个”“该问题”），必须明确写出问题对象或场景。**
   - ✅ 正例：为什么 **Kubernetes 节点** 在 CPU 利用率达到 90% 时仍然无法触发自动扩容？
   - ❌ 反例：为什么 **它** 在高负载时没有触发自动扩容？
2. 问题应清晰、具体，可独立存在，避免重复。
3. 输出为 JSON 数组

{% if extra_prompt %}
附加说明：
{{extra_prompt}}
{% endif %}