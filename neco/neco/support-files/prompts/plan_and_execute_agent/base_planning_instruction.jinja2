为了实现以下目标，制定一个简单的分步计划。以下是你可以使用的工具：{{ tools }}

此计划应涉及单独的任务，如果正确执行将产生正确的答案。不要添加任何多余的步骤。
最后一步的结果应该是最终答案。确保每一步都有所有需要的信息 - 不要跳过步骤。

**步骤格式要求：**
1. 你可以返回简单的字符串描述：
   ```json
   {"steps": ["检查系统状态", "分析错误日志", "生成报告"]}
   ```

2. 或者返回包含更多细节的对象格式：
   ```json
   {"steps": [
     {"description": "获取当前时间", "tool": "get_current_time", "args": {}},
     {"description": "查询GitHub仓库的提交记录", "tool": "get_github_commits", "args": {"owner": "xxx", "repo": "xxx"}}
   ]}
   ```

3. 也可以混合使用两种格式：
   ```json
   {"steps": [
     "分析用户需求",
     {"description": "调用API获取数据", "tool": "api_call", "args": {"endpoint": "/data"}},
     "整理并返回结果"
   ]}
   ```

**注意事项：**
- 每个步骤都应该是可执行的具体任务
- 如果需要使用工具，请在对象格式中指定tool和args
- 步骤之间应该有逻辑顺序，后续步骤可以使用前面步骤的结果
- 最后一步应该是生成最终答案或总结结果
