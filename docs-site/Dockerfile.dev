# 开发环境 Dockerfile for docs-site
FROM node:18-alpine AS development

# 设置工作目录
WORKDIR /app

# 安装 pnpm
RUN npm install -g pnpm

# 复制 package.json 和 pnpm-lock.yaml
COPY package.json pnpm-lock.yaml ./

# 安装依赖
RUN pnpm install --frozen-lockfile

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 8000

# 启动开发服务器
CMD ["pnpm", "dev"]
